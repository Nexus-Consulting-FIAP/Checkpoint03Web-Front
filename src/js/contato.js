const submit_result = document.getElementById("submit_result")
const btn_enviar = document.getElementById("btn_enviar")
btn_enviar.addEventListener("click", ()=>{
    submit_result.textContent = "Enviado com sucesso!"
})
